асаламуалекум

это лююютый вайб месенджер;

вайб типо Vibe;
типо название;

типо не вайбер а вайб лютый;
прям кек лол ещкере ваще;

там короче компиляция;
```bash
go run server.go tcp 2222 
go run client.go localhost:2222
```                     
вот тут вот акуратненько да;
              если tailscale то пишешь адресс;
              на котором активирован сервер жоска;
          если нет то localhost: и порт рандомный 4 цифры желательно;
          
там короче файлы launch;
    они чтобы быстро все скачать;
    я еще конечно же не придумал как это быстро сделать поэтому;
    copy <-> paste код который нужен (в зависимости от устроиства);
        если непонимаешь на какой системе сидишь (ну прям ваще все плохо);
        тогда там launch.sh есть для всех типов устроиств;
    копируешь и жоско вставляешь;
        в созданный touch launch.sh
        и запускаешь (может это команда chmod +x launch.sh && ./launch.sh);
            а может и нет - там от типа устроиства зависит;
                поищи в инете -> мне лень у меня deeeeeepseeek неработает чета;
ну короче вот;

чатишся там с кемнибудь по локальной сети (чтото типа "по сетке с друзьями");

кстати небольшой факт:
    от меня тут написан только readme и все; 
        выхахывахыва;
    не ну а че;
    телеграм блокируют но я же не тупой да
        захотел создал
            мир технологий как никак
ладно
    приятного пользования

мб скоро файлы загружать можно будет и картинки
    хз как сделать так чтобы была проверка на взлом картинку (типо вирус-скример и тд)
    ну короче мб будет чето
